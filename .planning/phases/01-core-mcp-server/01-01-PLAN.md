---
phase: 01-core-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - .gitignore
  - init_vault.py
  - CLAUDE.md
  - setup.sh
  - ecosystem.config.js
  - README.md
autonomous: false
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05
user_setup:
  - service: cloudflared
    why: "Cloudflare Tunnel binary required to expose localhost:8000 over HTTPS at a persistent subdomain (INFRA-03)"
    env_vars: []
    dashboard_config:
      - task: "Create Cloudflare account, add domain, create a tunnel named 'vault' via dashboard, run 'cloudflared tunnel login'"
        location: "https://dash.cloudflare.com → Networks → Tunnels"
  - service: pm2
    why: "Process manager for auto-restart of vault-mcp and cloudflare-tunnel processes (INFRA-04)"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Project root contains all 7 root files specified by D-04 plus CLAUDE.md and .gitignore"
    - "requirements.txt pins the exact Phase 1 dependency set including the `mcp` SDK (NOT fastapi-mcp tadata package)"
    - "init_vault.py creates the entire vault directory tree from scratch and seeds wiki/index.md and wiki/log.md in the formats specified in PRD §3"
    - "CLAUDE.md in repo root contains the operational rules from PRD §7 verbatim and will be copied into the vault by init_vault.py"
    - "setup.sh creates a Python virtualenv, installs requirements, and scaffolds the Cloudflare Tunnel config template"
    - "ecosystem.config.js launches uvicorn via the virtualenv binary path (.venv/bin/uvicorn) and runs cloudflared tunnel run vault"
    - "User has explicitly approved every Phase 1 package install (legitimacy gate from RESEARCH.md)"
  artifacts:
    - path: "requirements.txt"
      provides: "Phase 1 Python dependency manifest"
      contains: "mcp"
    - path: ".env.example"
      provides: "Template for VAULT_SECRET and VAULT_PATH env vars"
      contains: "VAULT_SECRET="
    - path: ".gitignore"
      provides: "Excludes .env, .venv, __pycache__"
      contains: ".env"
    - path: "init_vault.py"
      provides: "One-shot vault scaffolding script per D-10"
      min_lines: 40
    - path: "CLAUDE.md"
      provides: "Operational rules — embedded as MCP instructions by server.py"
      contains: "Query Rules"
    - path: "setup.sh"
      provides: "venv setup + pip install + cloudflared tunnel scaffolding"
      min_lines: 20
    - path: "ecosystem.config.js"
      provides: "PM2 config for vault-mcp + cloudflare-tunnel processes"
      contains: ".venv/bin/uvicorn"
    - path: "README.md"
      provides: "Setup walkthrough including DNS + dashboard steps"
      contains: "OAuth"
  key_links:
    - from: "ecosystem.config.js"
      to: ".venv/bin/uvicorn"
      via: "PM2 script field"
      pattern: "\\.venv/bin/uvicorn"
    - from: "init_vault.py"
      to: "wiki/index.md and wiki/log.md"
      via: "file write in PRD §3 format"
      pattern: "wiki/(index|log)\\.md"
    - from: "init_vault.py"
      to: "CLAUDE.md (vault root)"
      via: "shutil.copy from project root"
      pattern: "shutil\\.copy.*CLAUDE\\.md"
---

<objective>
Establish the project's filesystem scaffold and operational tooling — every non-server file required by Phase 1. This plan creates the Python virtualenv conventions, the dependency manifest, the vault initialization script, the operational rules document (CLAUDE.md), and the deployment artifacts (setup.sh, ecosystem.config.js, README.md) so subsequent plans can implement server.py against a known environment.

Purpose: Lock in the project layout (D-01..D-04, D-09..D-11) and verify the legitimacy of every package before any install. The server cannot be written until requirements.txt, .env.example, and the vault layout exist.

Output: All 7 root files declared in D-04 plus CLAUDE.md and .gitignore; a runnable init_vault.py that produces a fresh vault directory tree; a setup.sh that bootstraps the venv and tunnel; and an approved package set ready for `pip install`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-mcp-server/01-CONTEXT.md
@.planning/phases/01-core-mcp-server/01-RESEARCH.md
@PRD.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification gate</name>
  <files>requirements.txt (planned, not written until after approval)</files>
  <read_first>
    - .planning/phases/01-core-mcp-server/01-RESEARCH.md (Package Legitimacy Audit table — all rows tagged [ASSUMED])
  </read_first>
  <action>
    Pause execution. Surface the proposed Phase 1 package list to the user and wait for explicit "approved" reply before writing requirements.txt in Task 2. No file edits in this task — the only "action" is presenting the verification checklist and recording approval. The package list to present is exactly: fastapi==0.111.0, uvicorn[standard]==0.30.0, mcp (latest, NOT fastapi-mcp), python-dotenv==1.2.2, pydantic==2.7.0, python-frontmatter==1.1.0, watchdog==4.0.0, pytest, pytest-asyncio, httpx. The library correction from CONTEXT.md must be reiterated: `mcp` (Anthropic SDK), NOT `fastapi-mcp` (tadata).
  </action>
  <what-built>
    No code is written in this task. The packages slated for `requirements.txt` are: fastapi==0.111.0, uvicorn[standard]==0.30.0, mcp (latest, NOT fastapi-mcp), python-dotenv==1.2.2, pydantic==2.7.0, python-frontmatter==1.1.0, watchdog==4.0.0, pytest, pytest-asyncio, httpx. RESEARCH.md tagged every row [ASSUMED] because slopcheck was unavailable. Per the planner-source-audit gate, an explicit human verification is required before any install.
  </what-built>
  <how-to-verify>
    For each package below, open the corresponding registry URL and confirm: (a) the package name resolves, (b) the source repo link points to a recognized GitHub org, (c) recent release activity exists. Then approve the install.
    1. https://pypi.org/project/fastapi/ → repo github.com/fastapi/fastapi (Tiangolo)
    2. https://pypi.org/project/uvicorn/ → repo github.com/encode/uvicorn (encode org)
    3. https://pypi.org/project/mcp/ → repo github.com/modelcontextprotocol/python-sdk (Anthropic-affiliated)
    4. https://pypi.org/project/python-dotenv/ → repo github.com/theskumar/python-dotenv
    5. https://pypi.org/project/pydantic/ → repo github.com/pydantic/pydantic
    6. https://pypi.org/project/python-frontmatter/ → repo github.com/eyeseast/python-frontmatter
    7. https://pypi.org/project/watchdog/ → repo github.com/gorakhargosh/watchdog
    8. https://pypi.org/project/pytest/
    9. https://pypi.org/project/pytest-asyncio/
    10. https://pypi.org/project/httpx/ → repo github.com/encode/httpx

    CRITICAL: confirm that `mcp` (Anthropic SDK) is being installed and NOT `fastapi-mcp` (tadata-org). The tadata package only exposes FastAPI routes as MCP tools and cannot register custom functions — wrong tool for this project (per CONTEXT.md library correction).
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 packages have been confirmed legitimate, or list any package to substitute.</resume-signal>
  <verify>
    <human-check>User has visited each pypi.org URL and confirmed source repository legitimacy</human-check>
  </verify>
  <acceptance_criteria>
    - User response is exactly "approved" (or equivalent affirmative) before Task 2 runs
    - The package list approved matches the list embedded in this task (no silent substitutions)
    - `mcp` is included; `fastapi-mcp` is NOT included
  </acceptance_criteria>
  <done>User has confirmed all 10 packages and explicitly approved proceeding with `pip install`.</done>
</task>

<task type="auto">
  <name>Task 2: Create dependency manifest, env template, and gitignore</name>
  <files>requirements.txt, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-core-mcp-server/01-RESEARCH.md (Standard Stack table — exact version pins)
    - .planning/phases/01-core-mcp-server/01-CONTEXT.md (D-02 env var names; D-04 root file list)
    - PRD.md §6.1 (Phase 1 package list)
  </read_first>
  <action>
    Create requirements.txt pinning these exact packages and versions per D-04 and RESEARCH.md Standard Stack: fastapi==0.111.0, uvicorn[standard]==0.30.0, mcp (no pin — latest stable from PyPI), python-dotenv==1.2.2, pydantic==2.7.0, python-frontmatter==1.1.0, watchdog==4.0.0, pytest, pytest-asyncio, httpx. Do NOT include fastapi-mcp (tadata package) — the official `mcp` SDK provides FastMCP via `from mcp.server.fastmcp import FastMCP` (per CONTEXT.md library correction overriding D-05).

    Create .env.example with exactly these keys and empty placeholder values (no real secret): VAULT_SECRET=, VAULT_PATH=, OAUTH_CLIENT_ID=, OAUTH_REDIRECT_URI=. Include a comment header explaining VAULT_PATH must be an absolute path to a directory that does NOT yet exist (per D-11) and VAULT_SECRET acts as both the bearer token AND the OAuth client secret per CONTEXT.md decision 3.

    Create .gitignore with at minimum these patterns: .env, .venv/, __pycache__/, *.pyc, .pytest_cache/, .DS_Store, ~/.cloudflared/*.json.
  </action>
  <verify>
    <automated>test -f requirements.txt && test -f .env.example && test -f .gitignore && grep -q '^mcp' requirements.txt && ! grep -q 'fastapi-mcp' requirements.txt && grep -q 'VAULT_SECRET=' .env.example && grep -q 'VAULT_PATH=' .env.example && grep -q '^\.env$' .gitignore && grep -q '^\.venv/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains a line beginning with `mcp` and does NOT contain `fastapi-mcp`
    - requirements.txt contains exact pins: `fastapi==0.111.0`, `uvicorn[standard]==0.30.0`, `python-dotenv==1.2.2`, `pydantic==2.7.0`, `python-frontmatter==1.1.0`, `watchdog==4.0.0`
    - requirements.txt contains `pytest`, `pytest-asyncio`, `httpx` (unpinned acceptable)
    - .env.example contains lines `VAULT_SECRET=`, `VAULT_PATH=`, `OAUTH_CLIENT_ID=`, `OAUTH_REDIRECT_URI=`
    - .env.example values are blank placeholders (no real secret leaks into git)
    - .gitignore contains `.env`, `.venv/`, `__pycache__/`
  </acceptance_criteria>
  <done>All three files exist with the exact contents specified; the verify command exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Author CLAUDE.md operational rules and init_vault.py scaffolding script</name>
  <files>CLAUDE.md, init_vault.py</files>
  <read_first>
    - PRD.md §7 (CLOUD.md operational rules content — Identity, Query Rules, Ingest Rules, Lint Rules, Storage Decisions, What Never to Do, Ingestion Frontmatter Template)
    - PRD.md §3 (Vault Structure, index.md format, log.md format, frontmatter template)
    - .planning/phases/01-core-mcp-server/01-CONTEXT.md (D-07 file name correction: CLAUDE.md not CLOUD.md; D-10 init_vault.py responsibilities; D-11 VAULT_PATH must not exist)
    - .planning/phases/01-core-mcp-server/01-RESEARCH.md (init_vault.py skeleton in Code Examples section)
  </read_first>
  <action>
    Create CLAUDE.md in project root containing the operational rules from PRD §7 verbatim. The file title must be `# CLAUDE.md` (per D-07 override of PRD's CLOUD.md naming). Include all six sections from PRD §7: Identity, Query Rules, Ingest Rules, Ingestion Frontmatter Template, Lint Rules, Storage Decisions, What Never to Do. This file will be (a) read by server.py at startup and passed as the `instructions=` argument to `FastMCP(...)` (INFRA-05) and (b) copied by init_vault.py into the vault root.

    Create init_vault.py with these responsibilities per D-10: read VAULT_PATH from environment; create the directory at VAULT_PATH with `mkdir(parents=True, exist_ok=False)` so it fails if the directory already exists (per D-11); create the full subdirectory tree raw/webpages, raw/transcripts, raw/videos, raw/documents, raw/assets, raw/sources, wiki/entities, wiki/concepts, wiki/sources, wiki/queries; write wiki/index.md with the exact PRD §3 header format `# Vault Index\nLast updated: YYYY-MM-DD | Total pages: 0 | Total sources: 0\n\n## Entities\n\n## Concepts\n\n## Sources\n\n## Queries\n`; write wiki/log.md with content `# Vault Log\n`; use `shutil.copy` to copy CLAUDE.md from the project root (Path(__file__).parent / "CLAUDE.md") into the vault root. Script must be runnable as `python init_vault.py` after `.env` is sourced.
  </action>
  <verify>
    <automated>test -f CLAUDE.md && test -f init_vault.py && grep -q '^# CLAUDE\.md' CLAUDE.md && grep -q 'Query Rules' CLAUDE.md && grep -q 'Ingest Rules' CLAUDE.md && grep -q 'Never modify' CLAUDE.md && grep -q 'VAULT_PATH' init_vault.py && grep -q 'wiki/index\.md' init_vault.py && grep -q 'wiki/log\.md' init_vault.py && grep -q 'shutil.copy' init_vault.py && grep -q 'exist_ok=False' init_vault.py && python -c "import ast; ast.parse(open('init_vault.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md starts with `# CLAUDE.md` (per D-07; NOT `# CLOUD.md`)
    - CLAUDE.md contains the section headings: `Query Rules`, `Ingest Rules`, `Ingestion Frontmatter Template`, `Lint Rules`, `Storage Decisions`, `What Never to Do`
    - CLAUDE.md "What Never to Do" section contains text indicating raw/ files must never be modified
    - init_vault.py parses as valid Python (`python -c "import ast; ast.parse(...)"` exits 0)
    - init_vault.py uses `os.environ["VAULT_PATH"]` (raises KeyError if not set)
    - init_vault.py creates all 10 subdirectories (raw/webpages, raw/transcripts, raw/videos, raw/documents, raw/assets, raw/sources, wiki/entities, wiki/concepts, wiki/sources, wiki/queries)
    - init_vault.py calls `mkdir(... exist_ok=False)` on VAULT_PATH so reinit fails fast (per D-11)
    - init_vault.py writes wiki/index.md beginning with `# Vault Index`
    - init_vault.py writes wiki/log.md beginning with `# Vault Log`
    - init_vault.py uses `shutil.copy` to place CLAUDE.md at vault root
  </acceptance_criteria>
  <done>Both files exist; CLAUDE.md mirrors PRD §7 content; init_vault.py is parseable Python that performs the D-10 responsibilities.</done>
</task>

<task type="auto">
  <name>Task 4: Author setup.sh, ecosystem.config.js, and README.md</name>
  <files>setup.sh, ecosystem.config.js, README.md</files>
  <read_first>
    - .planning/phases/01-core-mcp-server/01-CONTEXT.md (D-09 setup.sh responsibilities, D-04 root files)
    - .planning/phases/01-core-mcp-server/01-RESEARCH.md (Environment Availability — virtualenv requirement; ecosystem.config.js example in Code Examples)
    - PRD.md §6.5 (PM2 process names: vault-mcp, cloudflare-tunnel)
    - PRD.md §2.2 (cloudflared config path ~/.cloudflared/config.yml)
  </read_first>
  <action>
    Create setup.sh as an executable bash script (start with `#!/usr/bin/env bash` and `set -euo pipefail`). It MUST: (1) check Python 3.11+ is available; (2) create a virtualenv at `.venv` with `python3 -m venv .venv` (per CONTEXT.md decision 4 — system Python is externally managed on macOS); (3) activate the venv and run `pip install -r requirements.txt`; (4) check `cloudflared` is on PATH and exit with an error message pointing to `brew install cloudflared` if not; (5) emit a `~/.cloudflared/config.yml` template (only if file does not exist) containing `tunnel: <TUNNEL-UUID>`, `credentials-file: ~/.cloudflared/<TUNNEL-UUID>.json`, an ingress rule mapping `vault.<your-domain>` to `http://localhost:8000`, and a fallback `service: http_status:404`; (6) print final instructions to (a) run `cloudflared tunnel login`, (b) run `cloudflared tunnel create vault`, (c) replace placeholders in config.yml, (d) populate .env, (e) run `python init_vault.py`, (f) run `pm2 start ecosystem.config.js`. The script must NOT actually create the tunnel or run cloudflared login — those require user interaction.

    Create ecosystem.config.js exporting `module.exports = { apps: [...] }` with two apps. App 1: name "vault-mcp", script ".venv/bin/uvicorn" (full venv path per CONTEXT.md decision 4), args "server:app --host 0.0.0.0 --port 8000", interpreter "none", cwd set via `process.cwd()` or literal project path, autorestart true, max_restarts 10, restart_delay 4000. App 2: name "cloudflare-tunnel", script "cloudflared" (resolved via PATH), args "tunnel run vault", interpreter "none", autorestart true, max_restarts 10, restart_delay 4000.

    Create README.md with these sections in order: Project overview (1 paragraph); Prerequisites (Python 3.11+, Node.js, Homebrew, Cloudflare account with a domain); Quick Start (run setup.sh, populate .env, run init_vault.py, run pm2 start ecosystem.config.js); Cloudflare Tunnel DNS section (steps that cannot be scripted: tunnel login, create tunnel, route DNS via dashboard or `cloudflared tunnel route dns vault vault.<domain>`); OAuth Connector section (claude.ai mobile requires OAuth 2.0 with PKCE — point at /authorize and /token endpoints from Plan 03, VAULT_SECRET serves dual role as bearer secret and OAuth client secret per CONTEXT.md decision 3); Troubleshooting (PM2 logs, common venv issues, tunnel auth errors).
  </action>
  <verify>
    <automated>test -f setup.sh && test -x setup.sh && test -f ecosystem.config.js && test -f README.md && grep -q 'python3 -m venv .venv' setup.sh && grep -q 'pip install -r requirements.txt' setup.sh && grep -q 'cloudflared' setup.sh && grep -q '\.venv/bin/uvicorn' ecosystem.config.js && grep -q 'vault-mcp' ecosystem.config.js && grep -q 'cloudflare-tunnel' ecosystem.config.js && grep -q 'autorestart: true' ecosystem.config.js && grep -q 'OAuth' README.md && grep -q 'Cloudflare Tunnel' README.md && node -e "require('./ecosystem.config.js')"</automated>
  </verify>
  <acceptance_criteria>
    - setup.sh is executable (test -x exits 0)
    - setup.sh first line is `#!/usr/bin/env bash` and second line includes `set -euo pipefail`
    - setup.sh contains literal string `python3 -m venv .venv`
    - setup.sh contains `pip install -r requirements.txt`
    - setup.sh checks for cloudflared and emits ~/.cloudflared/config.yml template
    - ecosystem.config.js is valid JavaScript (`node -e "require(...)"` exits 0)
    - ecosystem.config.js contains `.venv/bin/uvicorn` literal (NOT just `uvicorn`)
    - ecosystem.config.js exports apps named `vault-mcp` and `cloudflare-tunnel` (both)
    - ecosystem.config.js sets `autorestart: true` for both apps
    - README.md contains section discussing OAuth (so the claude.ai mobile gap from CONTEXT.md is documented)
    - README.md contains "Cloudflare Tunnel" section explaining DNS dashboard steps
  </acceptance_criteria>
  <done>All three files exist; setup.sh runs without syntax errors; ecosystem.config.js loads in Node; README covers OAuth + Cloudflare DNS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project filesystem → vault filesystem | init_vault.py creates a fresh tree; bug here would create files outside intended VAULT_PATH if env var is malformed |
| PyPI → local environment | `pip install` from requirements.txt pulls third-party code into the venv |
| Cloudflare dashboard → ~/.cloudflared/config.yml | Tunnel credentials file is sensitive; .gitignore must exclude it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | requirements.txt (supply chain) | mitigate | Task 1 is a blocking-human checkpoint requiring explicit approval of every package by visiting pypi.org and confirming the source repo |
| T-01-02 | Tampering | init_vault.py VAULT_PATH | mitigate | init_vault.py uses `mkdir(exist_ok=False)` so it fails fast on an existing directory rather than co-mingling with an unrelated tree (per D-11) |
| T-01-03 | Information Disclosure | .env file leaks into git | mitigate | .gitignore contains `.env` line; .env.example holds only blank placeholders |
| T-01-04 | Information Disclosure | Cloudflare tunnel credentials JSON | mitigate | .gitignore contains `~/.cloudflared/*.json` pattern (also default location is outside repo) |
| T-01-SC | Tampering | npm/pip installs (Phase 1 packages) | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] (Task 1) |
</threat_model>

<verification>
After all tasks complete, the project root should contain: requirements.txt, .env.example, .gitignore, init_vault.py, CLAUDE.md, setup.sh (executable), ecosystem.config.js, README.md. The user has explicitly approved the package list. No actual installs have been performed yet — that happens in Plan 03 once server.py exists.

End-to-end smoke verification (informational, will be confirmed in Plan 07):
- `bash setup.sh` runs to completion (after `cloudflared` is brew-installed) and produces .venv/ + ~/.cloudflared/config.yml template.
- `python init_vault.py` with VAULT_PATH set to a fresh path produces the full directory tree and seed files.
</verification>

<success_criteria>
- All 8 root files exist with correct content per D-04, D-07, D-09, D-10
- requirements.txt uses `mcp` SDK and excludes `fastapi-mcp` (per CONTEXT.md library correction)
- init_vault.py is parseable Python and implements the D-10 contract
- CLAUDE.md content matches PRD §7 with the correct title (D-07)
- ecosystem.config.js references `.venv/bin/uvicorn` (per CONTEXT.md decision 4)
- README.md documents both Cloudflare DNS setup and the OAuth requirement for claude.ai mobile
- User has approved every package in the dependency manifest
</success_criteria>

<output>
Create `.planning/phases/01-core-mcp-server/01-01-SUMMARY.md` when done.
</output>
