---
phase: 01-core-mcp-server
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_smoke.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "pytest is configured for asyncio mode and discovers tests under tests/"
    - "A shared `tmp_vault` fixture produces a fully-seeded temporary vault on every test invocation"
    - "A trivial smoke test exits 0, proving the test harness itself works before any production code is written"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest config (asyncio_mode, testpaths)"
      contains: "[tool.pytest"
    - path: "tests/conftest.py"
      provides: "tmp_vault fixture and shared test helpers"
      contains: "tmp_vault"
    - path: "tests/test_smoke.py"
      provides: "Harness self-test"
      contains: "def test_smoke"
  key_links:
    - from: "pyproject.toml"
      to: "tests/"
      via: "testpaths configuration"
      pattern: "testpaths"
    - from: "tests/conftest.py"
      to: "init_vault.py seed format"
      via: "fixture replicates wiki/index.md and wiki/log.md seed content"
      pattern: "wiki/index\\.md|wiki/log\\.md"
---

<objective>
Establish the test harness that all subsequent plans (server bootstrap, navigation, retrieval, ingestion, maintenance) will rely on. The harness must exist before server.py is written so every tool can be developed with verifyable acceptance criteria backed by pytest.

Purpose: Eliminate the "Wave 0" placeholder gap declared in RESEARCH.md and VALIDATION.md. After this plan, every later task can name a concrete pytest target rather than `MISSING — Wave 0 must create ...`.

Output: pyproject.toml with pytest config; tests/conftest.py with a `tmp_vault` fixture; tests/test_smoke.py proving the harness runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-mcp-server/01-RESEARCH.md
@.planning/phases/01-core-mcp-server/01-VALIDATION.md
@PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure pytest and create smoke test</name>
  <files>pyproject.toml, tests/__init__.py, tests/test_smoke.py</files>
  <read_first>
    - .planning/phases/01-core-mcp-server/01-VALIDATION.md (test framework table)
    - .planning/phases/01-core-mcp-server/01-RESEARCH.md (Validation Architecture)
  </read_first>
  <action>
    Create pyproject.toml with a `[tool.pytest.ini_options]` table containing: `testpaths = ["tests"]`, `asyncio_mode = "auto"` (so async tests don't need an explicit decorator), `python_files = "test_*.py"`, and `addopts = "-ra"`. If pyproject.toml already exists (it should not at this point in greenfield), preserve unrelated tables and only add/overwrite the pytest table.

    Create tests/__init__.py as an empty file (marks tests as a package so conftest fixtures resolve correctly when pytest is invoked from project root).

    Create tests/test_smoke.py containing a single sync test function `def test_smoke():` that asserts `1 + 1 == 2`. Purpose: verify the harness itself runs before any production code exists.
  </action>
  <verify>
    <automated>test -f pyproject.toml && test -f tests/__init__.py && test -f tests/test_smoke.py && grep -q 'tool.pytest' pyproject.toml && grep -q 'asyncio_mode' pyproject.toml && grep -q 'testpaths' pyproject.toml && .venv/bin/pytest tests/test_smoke.py -x -q 2>/dev/null || python -m pytest tests/test_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists and contains `[tool.pytest.ini_options]` table
    - pyproject.toml `asyncio_mode = "auto"` line present
    - pyproject.toml `testpaths = ["tests"]` line present
    - tests/__init__.py exists (may be empty)
    - tests/test_smoke.py contains `def test_smoke` and asserts a true statement
    - `pytest tests/test_smoke.py -x` exits 0 once pytest is installed (test-running depends on Plan 03 venv but parse validity is checked above)
  </acceptance_criteria>
  <done>pytest config and smoke test exist; smoke test passes when pytest is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Author shared tmp_vault fixture in conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - PRD.md §3 (vault structure, index.md format, log.md format, frontmatter template)
    - .planning/phases/01-core-mcp-server/01-CONTEXT.md (D-10 init_vault.py responsibilities — fixture must mirror its output)
    - tests/test_smoke.py (verify import patterns)
  </read_first>
  <action>
    Create tests/conftest.py exposing a pytest fixture named `tmp_vault` (function-scoped, returns `pathlib.Path` to a fresh vault root). Implementation: use the `tmp_path` builtin fixture as the parent directory, create a subdirectory `vault/`, then create the full directory tree that init_vault.py produces (raw/webpages, raw/transcripts, raw/videos, raw/documents, raw/assets, raw/sources, wiki/entities, wiki/concepts, wiki/sources, wiki/queries). Write wiki/index.md with the seed content `# Vault Index\nLast updated: 2026-05-29 | Total pages: 0 | Total sources: 0\n\n## Entities\n\n## Concepts\n\n## Sources\n\n## Queries\n` and wiki/log.md with `# Vault Log\n`. Write a CLAUDE.md placeholder file at the vault root containing the literal text `# CLAUDE.md\n\nTest instructions placeholder.\n` (server.py reads this at startup; tests need it present). Yield the vault path. No cleanup required — `tmp_path` handles it.

    Add a second fixture named `vault_env` (function-scoped) that uses pytest's `monkeypatch` fixture to set `os.environ["VAULT_PATH"] = str(tmp_vault)` and `os.environ["VAULT_SECRET"] = "test-secret-token"` and `os.environ["OAUTH_CLIENT_ID"] = "test-client"` and `os.environ["OAUTH_REDIRECT_URI"] = "http://localhost/cb"`. Return None. Tests that need a configured environment depend on both `tmp_vault` and `vault_env`.

    Add a small helper at module level: `def make_note(vault: Path, rel: str, frontmatter: dict, body: str) -> Path` that writes a markdown file at vault/rel with `---\n{yaml}\n---\n\n{body}` formatting. Used by later tests to seed vault content. Use stdlib yaml-like writes (manual serialization of the dict — keys=values, lists as `[item1, item2]`) or `import yaml` only if pyyaml is available; otherwise hand-serialize the minimal dict types the tests will use (strings, lists of strings, dates). Prefer python-frontmatter for serialization once that package is installed — wrap the helper to use `frontmatter.Post(content=body, **frontmatter)` then `frontmatter.dumps(post)` if `frontmatter` import succeeds, falling back to manual serialization in an ImportError handler.
  </action>
  <verify>
    <automated>test -f tests/conftest.py && grep -q 'def tmp_vault' tests/conftest.py && grep -q 'def vault_env' tests/conftest.py && grep -q 'def make_note' tests/conftest.py && grep -q 'wiki/index\.md\|wiki/index.md\|"wiki", "index.md"' tests/conftest.py && python -c "import ast; ast.parse(open('tests/conftest.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py parses as valid Python (`ast.parse` succeeds)
    - tests/conftest.py defines a `tmp_vault` pytest fixture
    - tests/conftest.py defines a `vault_env` pytest fixture that sets VAULT_PATH, VAULT_SECRET, OAUTH_CLIENT_ID, OAUTH_REDIRECT_URI via monkeypatch
    - tests/conftest.py defines a `make_note(vault, rel, frontmatter, body)` helper
    - `tmp_vault` fixture creates all 10 subdirectories (raw/webpages, raw/transcripts, raw/videos, raw/documents, raw/assets, raw/sources, wiki/entities, wiki/concepts, wiki/sources, wiki/queries)
    - `tmp_vault` writes wiki/index.md beginning with `# Vault Index`
    - `tmp_vault` writes wiki/log.md beginning with `# Vault Log`
    - `tmp_vault` writes a CLAUDE.md file at the vault root
  </acceptance_criteria>
  <done>conftest.py exists; `tmp_vault`, `vault_env`, and `make_note` are defined; module imports cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → tmp_path | Fixture writes to a pytest-managed temp dir; cleanup is automatic |
| Test process → real OS env | `vault_env` uses monkeypatch so env changes are reverted per-test (no leakage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tests/conftest.py polluting real env | mitigate | Use `monkeypatch` fixture (auto-reverts) instead of direct `os.environ` mutation |
| T-02-02 | Information Disclosure | Test VAULT_SECRET leaking into logs | accept | Test token is the literal string `"test-secret-token"` — not a real secret |
</threat_model>

<verification>
Run `pytest tests/test_smoke.py -x -q` (after Plan 03 installs deps). Expect: 1 passed.
Run `pytest --collect-only tests/` and confirm `tmp_vault` and `vault_env` fixtures are visible to test functions in tests/.
</verification>

<success_criteria>
- pytest discovers tests under tests/ with asyncio_mode auto
- `tmp_vault` fixture produces a fully-seeded vault directory equivalent to init_vault.py output
- Smoke test passes once pytest is installed
- No pre-existing pyproject.toml or test file is overwritten without preserving unrelated content
</success_criteria>

<output>
Create `.planning/phases/01-core-mcp-server/01-02-SUMMARY.md` when done.
</output>
